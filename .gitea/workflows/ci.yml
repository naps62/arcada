name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    # Pinned to the toolchain this project actually needs: Elixir 1.15+ on OTP 25
    # (apt's Elixir 1.14 is too old for current Hex deps). The official image
    # bundles full OTP, so mint's public_key/ssl shims compile out of the box.
    container:
      image: elixir:1.17.3-otp-25

    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
        options: >-
          --health-cmd "pg_isready -U postgres"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    env:
      MIX_ENV: test
      DATABASE_HOST: postgres
      DATABASE_USER: postgres
      DATABASE_PASS: postgres

    steps:
      - uses: actions/checkout@v4

      # git is needed to fetch the heroicons GitHub dependency.
      - name: Install git
        run: apt-get update && apt-get install -y --no-install-recommends git

      - name: Install hex + rebar
        run: |
          mix local.hex --force
          mix local.rebar --force

      - name: Cache deps and build
        uses: actions/cache@v4
        with:
          path: |
            deps
            _build
          key: ${{ runner.os }}-mix-${{ hashFiles('mix.lock') }}
          restore-keys: ${{ runner.os }}-mix-

      - name: Fetch dependencies
        run: mix deps.get

      - name: Check formatting
        run: mix format --check-formatted

      - name: Compile (warnings as errors)
        run: mix compile --warnings-as-errors

      - name: Run tests
        run: mix test
